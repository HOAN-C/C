#include<stdio.h>
#include<stdio.h>
int total(int kor, int eng, int mat)

{


}
int main(){
  int kor, eng, mat;
  int tot;
  printf("Enter 3 subject");
  scanf("%d%d%d", &kor, &eng, &mat);
  tot = kor + eng + mat;
  return tot;
}
